@require 'nib'

json('../../stylus/vars.json')

// Squish individual stars together a bit more
.z-stars
  font-size: $iconTiny

  &.hidden
    visibility: hidden

  .icon-star-half-fill:before, .icon-star:before
    margin: 0 0.15rem
