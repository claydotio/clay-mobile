@require 'nib'

json('../../stylus/vars.json')

.z-legacy-banner
  background: $grey900
  line-height: 30px
  color: $white
  font-size: $fontSizeSmall
  text-align: center

  > .link
    color: inherit
    text-decoration: underline
