@require 'nib'
@require 'zorium-paper/base'

json('../../stylus/vars.json')

.z-request-avatar-card_content
  > .title
    zp-font-headline()
    font-weight: lighter
    margin: 0 0 12px 0

  > .description
    zp-font-subhead()
    margin: 0 0 16px 0

  > .actions
    text-align: right

    > .clickable
      display: inline-block
