json('../../stylus/vars.json')

@require 'nib'

$zIndexModalClose = 1

.z-modal-close
  position: absolute
  top: 10px
  right: 10px
  opacity: 0.75
  z-index: $zIndexModalClose

  > .icon-close
    color: $greyMedium
    font-size: $fontSizeLarge
