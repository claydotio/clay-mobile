@require 'nib'
@require '../../node_modules/zorium-paper/base.styl'

json('../../stylus/vars.json')

.z-join-thanks-card_content
  > .thanks
    zp-font-headline()
    color: $white

  > .description
    zp-font-subhead()
    color: $white
  > .actions
    margin-top: 8px
    text-align: right
